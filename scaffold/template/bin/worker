#!/usr/bin/env bash
set -euo pipefail
export SETTINGS_PATH="${SETTINGS_PATH:-app/settings.toml}"

RELEASE_BIN="./target/release/worker"
if [[ -x "${RELEASE_BIN}" ]]; then
    exec "${RELEASE_BIN}" "$@"
fi
cargo run -p rustforge-starter --bin worker -- "$@"
